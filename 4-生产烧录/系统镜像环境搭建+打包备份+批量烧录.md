# 系统镜像环境搭建 + 打包备份 + 批量烧录

## 一、NVIDIA JetPack 初始系统烧录

### 1.1 烧录工具

| 开发板类型 | 工具 | 说明 |
| --- | --- | --- |
| EMMC 开发板 | NVIDIA SDK Manager | 初次在线烧录，时间较长 |
| SD 卡开发板 | NVIDIA SDK Manager | 初次在线烧录，时间较长 |
| SD 卡开发板 | Balena Etcher | 离线烧录，批量复制 |

### 1.2 NVIDIA SDK Manager 烧录步骤

1. 设备接线：...
2. Ubuntu 电脑环境准备：...
3. 选择安装对应版本的 JetPack SDK：勾选 DeepStream 和其它 AI 库，默认带有 CUDA、TensorRT、CuDNN、OpenCV、Gstreamer 等
4. 设置系统初始账户：用户名 `skyfend`，密码 `123456`

### 1.3 SD 卡开发板烧录步骤

...

## 二、系统环境配置

1. 安装基础工具包：
```
# -- jtop
sudo apt update
sudo apt install python3-pip
sudo pip3 install -U jetson-stats
sudo systemctl restart jetson-stats

# -- sqlite3、tcpdump、cmake
sudo apt install sqlite3 
sudo apt install tcpdump 
sudo apt install cmake

# -- 补充gstreamer的 rtsp server 库
sudo apt update
sudo apt install libgstrtspserver-1.0-dev gstreamer1.0-rtsp

```

2. 安装 ROS2 Humble

## 三、生产系统镜像打包备份

...

## 四、批量烧录

...

## 五、系统迁移固态硬盘（可选）

...
