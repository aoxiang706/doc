# 中程雷视标定开发——现状分析、问题诊断与改进方案

> 基于 `ptz100_agx_fix`（当前运行代码）与 `ptz100_agx`（需求文档及参考代码）两个工程的深度对比分析。
>
> 生成日期: 2026-02-27

---

## 目录

1. [概述与目标](#1-概述与目标)
2. [系统架构与模块职责](#2-系统架构与模块职责)
3. [端到端标定流程现状](#3-端到端标定流程现状)
   - 3.1 [用户设置初值 (0x71)](#31-用户设置初值-0x71)
   - 3.2 [启动标定 (0x94 / 0x9A)](#32-启动标定-0x94--0x9a)
   - 3.3 [PTZ锁定无人机](#33-ptz锁定无人机)
   - 3.4 [雷达目标选择与采样](#34-雷达目标选择与采样)
   - 3.5 [标定完成与结果确认](#35-标定完成与结果确认)
4. [Alink协议消息汇总](#4-alink协议消息汇总)
5. [标定状态机](#5-标定状态机)
6. [核心算法分析](#6-核心算法分析)
   - 6.1 [目标筛选 (radar_2ptz_calib)](#61-目标筛选-radar_2ptz_calib)
   - 6.2 [引导采样 (radarGuide_calib)](#62-引导采样-radarguide_calib)
   - 6.3 [外参求解 (mainAPI / calib.cpp)](#63-外参求解-mainapi--calibcpp)
7. [数据持久化与运行期应用](#7-数据持久化与运行期应用)
8. [需求与代码差距分析（GAP Analysis）](#8-需求与代码差距分析gap-analysis)
9. [改进方案](#9-改进方案)
10. [实施路线图](#10-实施路线图)
11. [附录](#11-附录)

---

## 1. 概述与目标

### 1.1 背景

PTZ（光电转台）在远距离工作时视场角极小（<1°），不具备主动探测能力，必须依赖雷达等设备提供引导角度信息。**雷视标定**的核心目的是：通过对比雷达检测角度与PTZ实际锁定角度的差异，求解并修正两者之间的安装偏差（外参），从而提高雷达→PTZ引导的精度。

前期已完成**四面阵近程雷达**的标定功能。中程雷达与近程雷达在探测距离、角度精度、波束特性上存在显著差异，需要针对中程雷达的特点对标定流程和算法进行适配与优化。

### 1.2 标定目标

| 维度 | 说明 |
|------|------|
| **待标定外参** | yaw_offset（方位偏差）、pitch_offset（俯仰偏差）、roll_offset（横滚偏差）、height_offset（高度偏差） |
| **精度指标** | 方位/俯仰残差均值 < 0.5°，方差 < 0.3° (目标值) |
| **支持拓扑** | 机扫中程雷达 + PTZ；四面阵中程雷达 + PTZ |
| **C2交互** | 与近程标定流程统型，复用 0x71 / 0x91 / 0x92 / 0x93 / 0x95 消息，新增 0x9A 统合启停/查询 |

---

## 2. 系统架构与模块职责

```
┌─────────┐  Alink协议    ┌──────────────────────────────────────────┐
│  C2客户端 │ ◄──────────► │                AGX嵌入式                  │
│ (上位机)  │  (TCP二进制)  │                                          │
└─────────┘               │  ┌──────────┐  ROS2  ┌───────────────┐  │
                          │  │ alink_srv │ ◄────► │  ros_app 桥接   │  │
                          │  │ (协议解析) │        │  (消息转换)    │  │
                          │  └──────────┘        └───────┬───────┘  │
                          │                              │          │
                          │              ┌───────────────┼────────┐ │
                          │              ▼               ▼        │ │
                          │  ┌──────────────┐  ┌──────────────┐   │ │
                          │  │ 自标定模块    │  │ PTZ引导模块   │   │ │
                          │  │ (calibr_alg) │  │ (ptz_guider) │   │ │
                          │  └──────┬───────┘  └──────────────┘   │ │
                          │         │                              │ │
                          │         ▼                              │ │
                          │  ┌──────────────┐  ┌──────────────┐   │ │
                          │  │ AI算法模块    │  │ SQLite / YAML │   │ │
                          │  │ (ai_alg)     │  │ (持久化存储)  │   │ │
                          │  └──────────────┘  └──────────────┘   │ │
                          └────────────────────────────────────────┘ │
```

### 各模块职责

| 模块 | 关键文件 | 职责 |
|------|---------|------|
| **alink_srv** | `alink_track.cpp/.h`, `alink_system.cpp` | 解析C2 Alink报文，转发给ROS层；上报标定状态/数据给C2 |
| **ros_app** | `ros_app.cpp/.h` | AGX内部ROS2消息桥接；订阅/发布标定状态(`UserCalibState`)、标定数据(`CalibRadarPtzObjList`)等 |
| **自标定模块** (calibr_alg) | ROS节点（需求文档描述） | 标定状态机管理、数据收集、外参求解、结果评估 |
| **PTZ引导模块** (ptz_guider) | ROS节点 | PTZ转向控制、锁定状态管理、引导/标定模式切换 |
| **AI算法模块** (ai_alg) | `ai_main.cpp`, `guide.cpp`, `calib.cpp`, `tf.cpp` | 雷达目标筛选、标定数据采集写文件、外参计算 |
| **SQLite/YAML** | `global_calibration.h/.c`, `radar_calibration.h/.c` | 全局标定参数和单雷达标定结果的持久化 |

---

## 3. 端到端标定流程现状

### 3.1 用户设置初值 (0x71)

**流程**：C2 → `alink_set_mark_param_new()` → 写入 SQLite (`write_global_calibration`) + 写入 YAML (`g_hostDevCfgApi.updateBySn`) → `ros_app_cmd_set(21, 1)` 通知自标定模块 → 自标定模块调用 `init_alg()` 读取初值 → 发布 `UserCalibState` → 嵌入式上报 C2。

**当前代码位置**：`alink_track.cpp:667-714`

**参数内容**：heading（朝向角）、longitude、latitude、altitude、rolling、pitching、relativeHeight

#### 🔴 发现的问题

1. **双写冗余**：同时写入 SQLite 和 YAML，注释中提到"以后不用这个表了，统一用yaml，短期暂时不删除"——但两年后仍未清理
2. **SN硬编码**：`strcpy(list.sn, "SRP100")` 硬编码了设备SN，不支持多设备场景
3. **缺少参数校验**：收到的坐标参数没有有效性检查（如经纬度范围、海拔合理性）

---

### 3.2 启动标定 (0x94 / 0x9A)

**需求**：使用新的 0x9A 消息统合启动、停止、添加/删除标定方位角、查询历史标定结果。

**当前代码**：仍使用旧的 0x94 (`user_start_radar_calibration`)，只支持"启动标定"功能。

```
C2 → 0x94(sn + reserve) → alink_track.cpp:952
  → ros_app_cmd_set(CMD_ID_LOCAL_CALIBRATION, STATE_CODE_CALIBRATION_USER_MODE_START)
  → 自标定模块检查雷达/PTZ在线 → CALIB_STATE_IDLE
  → 嵌入式收到IDLE后 → ros_app_calib_radar_info(calibration_sn) 发送SN给引导模块
  → 引导模块转动PTZ至雷达法线角度 → 应答 PTZ_ARRIVED
  → 自标定模块切换至 CALIB_STATE_PTZ_ARRIVED
```

#### 🔴 发现的问题

1. **0x9A 未实现**：需求文档定义了 0x9A 消息（含cmd=1启动/2停止/3添加方位/4删除方位/5查询历史），但代码中完全不存在
2. **0x96 功能有限**：当前 0x96 只是查询已有标定SQL数据，不支持按方位角添加/删除
3. **无"标定方位角"概念**：中程雷达可能需要按方位分段标定（0°/90°/180°/270°等），但当前代码没有方位角管理逻辑

---


### 3.3 PTZ锁定无人机

**流程**：

1. **自动锁定模式** (默认)：PTZ自动检测并锁定无人机 → 引导模块应答 `PTZ_TRACKING_FINISHED`
2. **手动锁定模式** (0x91 Request=4)：C2切换到手动模式 → 引导模块释放目标、启动AI检测 → 用户在C2界面手点像素框 → C2下发 0x95 → 引导模块锁定指定像素目标
3. **锁定确认/取消** (0x91 Request=1/2)：C2弹窗确认锁定的目标

**当前代码位置**：`alink_track.cpp:996-1037` (`radar_calibration_req`)

#### 🔴 发现的问题

1. **0x91 处理不完整**：需求定义了8种Request值，当前代码只实现了1-4，且：
   - Request=5（退出PTZ手动锁定）→ 返回 0xFF（未实现）
   - Request=6（标定结果取消）→ **缺失**
   - Request=7（手动选择雷达目标）→ **缺失**
   - Request=8（自动选择雷达目标）→ **缺失**
2. **映射关系错乱**：代码中 Request=3 映射到 `RESULT_CONFIRMED`，但需求文档中 Request=3 是"视觉锁定目标确认"，Request=7 才是"标定结果确认"。新老定义有冲突。

---

### 3.4 雷达目标选择与采样

**流程**：
- 自标定模块切换到 `CALIB_STATE_RADAR_MANUAL_SELECT` 或自动选择模式
- 用户在C2选择雷达靶机目标 → 0x81 → 嵌入式转发
- 自标定模块将雷达原始目标与手选ID匹配 → 坐标转换到PTZ坐标系
- 结合PTZ锁定角度，计算雷达-PTZ角度偏差
- 按1Hz发布 `CalibRadarPtzObjList` (0x93) 给C2显示曲线

**当前代码实现**（在 `ptz100_agx_fix` 中）：
- `ai_alg_calib()` 作为标定主函数
- `radarGuide_calib()` 选择雷达目标
- 数据写入文件 `radar_ptz_status.txt`
- 采集完成后启动 `mark_task` 线程调用 `mainAPI()` 计算外参

#### 🔴 发现的问题

1. **`STATE_CODE_CALIBRATION_USER_MODE_RADAR_MANUAL_SELECT` 未定义**：需求文档引用了此状态码，但代码中不存在
2. **雷达原始目标类型 0xE1 复用冲突**：需求说新增 0xE1 用于雷达原始目标，但代码中 0xE1 已被 `track_pkg_remote_id`（遥控ID）和 `HeartBeatHostStatus`（心跳）复用
3. **数据采样使用文件 I/O**：`ai_alg_calib()` 每个采样点都 `fopen/fprintf/fclose`，在高频采样下存在性能和可靠性隐患
4. **缺少中程雷达特化筛选**：`radar_2ptz_calib()` 仅靠 `alive >= 0` 过滤，没有距离/速度/仰角/信噪比门限

---

### 3.5 标定完成与结果确认

**流程**：
- 采样达到阈值或超时 → 启动 `mark_task` 线程
- `mainAPI()` 从文件读取采样数据 → 分段、合并、去异常值 → 计算方位/俯仰偏差均值
- 结果通过 `alink_upload_mark_result()` 上报C2
- C2弹窗确认 → 0x91 Request=3 → `RESULT_CONFIRMED`
- 嵌入式将结果写入YAML

#### 🔴 发现的问题

1. **结果未持久化到SQLite**：`mark_task` 计算完成后只上报C2，没有将 yaw_offset/pitch_offset 写入 `radar_calibration` 表
2. **结果未实时应用**：`setPTZ0()` 函数中没有加载标定偏差值进行修正
3. **自标定模块与AI算法模块职责交叉**：需求文档描述的流程由自标定模块（ROS节点）主导，但实际代码中标定逻辑在AI算法模块（`ai_alg_calib`），两个体系不一致

---

## 4. Alink协议消息汇总

| 消息ID | 方向 | 功能 | 代码状态 |
|--------|------|------|---------|
| **0x71** | C2→AGX→C2 | 设置标定初值（经纬度、朝向、高度等） | ✅ 已实现 |
| **0x81** | C2→AGX | 用户选择跟踪目标 | ✅ 已实现 |
| **0x82** | AGX→C2 | AGX上报跟踪状态 | ✅ 已实现 |
| **0x91** | C2→AGX→C2 | 标定确认（8种子命令） | ⚠️ 部分实现(仅1-4) |
| **0x92** | 双向 | 标定状态查询/上报 | ✅ 已实现 |
| **0x93** | AGX→C2 | 标定过程数据（PTZ/雷达角度对列表） | ✅ 已实现 |
| **0x94** | C2→AGX | 启动标定（旧） | ✅ 已实现（需迁移到0x9A） |
| **0x95** | C2→AGX→C2 | 用户下发手点像素目标值 | ✅ 已实现 |
| **0x96** | C2→AGX→C2 | 查询历史标定SQL数据 | ✅ 已实现（需整合到0x9A） |
| **0x9A** | C2→AGX→C2 | 启动/停止标定 + 管理/查询标定方位角 | ❌ 未实现 |
| **0xE1** | — | 雷达原始目标类型（新） | ❌ 未实现（ID已被占用） |
| **0xE6** | AGX→C2 | 融合新协议目标（含debug_status=30标识雷达原始数据） | ✅ 已实现 |

---

## 5. 标定状态机

```
                    ┌─────────────────────────────┐
                    │                             │
                    ▼                             │
    ┌──────────────────────┐                     │
    │  CALIB_STATE_IDLE(0) │◄────────────────────┤
    └──────────┬───────────┘  结果确认/取消/超时    │
               │ 启动标定                         │
               ▼                                 │
    ┌──────────────────────────┐                 │
    │ CALIB_STATE_INIT_PARAM   │                 │
    │     _SET_SUCCESS(1)      │                 │
    └──────────┬───────────────┘                 │
               │ 设备在线确认                      │
               ▼                                 │
    ┌──────────────────────────┐                 │
    │ CALIB_STATE_INITIALIZED  │                 │
    │          (2)             │                 │
    └──────────┬───────────────┘                 │
               │ 22=INITIALIZE_FAIL → 退出       │
               │ PTZ到达指定角度                   │
               ▼                                 │
    ┌──────────────────────────┐                 │
    │ CALIB_STATE_PTZ_ARRIVED  │                 │
    │          (3)             │                 │
    └──────────┬───────────────┘                 │
               │ PTZ锁定目标                      │
               ▼                                 │
    ┌──────────────────────────────┐             │
    │ CALIB_STATE_PTZ_TRACKING     │             │
    │     _SUCCESS(4)              │             │
    │ 24=PTZ_TRACKING_FAIL → 回退   │             │
    └──────────┬───────────────────┘             │
               │ 用户确认锁定                      │
               ▼                                 │
    ┌──────────────────────────────┐             │
    │ CALIB_STATE_PTZ_TRACKING     │             │
    │     _SUCCESS_CONFIRMED(5)    │             │
    └──────────┬───────────────────┘             │
               │ ⭐ 缺失: RADAR_MANUAL_SELECT     │
               │ 开始数据采集                      │
               ▼                                 │
    ┌──────────────────────────┐                 │
    │ CALIB_STATE_DOING(8)    │                 │
    └──────────┬───────────────┘                 │
               │ 进度100%或超时                    │
               ▼                                 │
    ┌──────────────────────────┐                 │
    │ CALIB_STATE_DONE(9)     │                 │
    └──────────┬───────────────┘                 │
               │ 确认(10)/取消(30)                 │
               ▼                                 │
    ┌──────────────────────────────────┐         │
    │ CALIB_STATE_RESULT_CONFIRMED(10) ├─────────┘
    │ 或 CALIB_STATE_RESULT_CANCELED(30)│
    └──────────────────────────────────┘
```

#### 🔴 状态机缺失项

- `CALIB_STATE_RADAR_MANUAL_SELECT`：需求中定义了用户手选雷达目标的状态，但代码中未定义
- `CALIB_STATE_RADAR_AUTO_SELECT`：自动选择雷达目标的状态也未定义
- 缺少明确的"方位段"概念——中程雷达可能需要分多个方位段分别标定

---

## 6. 核心算法分析

### 6.1 目标筛选 (radar_2ptz_calib)

**文件**：`ptz100_agx_fix/src/srv/algs/ai_alg/guide.cpp:203-263`

**当前逻辑**：
```
对所有雷达目标:
    if (alive >= 0):       // 唯一过滤条件
        加入 useful_obj

if useful_obj 只有1个:
    直接选定
else if 有多个:
    选择与当前锁定ID匹配的目标 (按 ptz_id 匹配)
    否则选择 alive 值最大（生存时间最长）的目标
```

#### 🔴 问题

| 问题 | 影响 | 建议 |
|------|------|------|
| 仅靠 `alive>=0` 过滤 | 杂波、地物、车辆等非目标也会通过 | 增加距离范围(200m-5km)、速度门限(>2m/s)、仰角门限(>0°)等中程特化筛选条件 |
| 无RCS/SNR门限 | 低质量探测点影响标定精度 | 若雷达上报SNR/RCS，增加门限过滤 |
| 未使用目标分类信息 | `classification` 字段未利用 | 仅选择 classification==无人机 的目标 |
| 多目标选择策略简单 | 可能误选非标定靶机 | 结合C2手选ID(0x81)做强关联，或要求标定时空域只有单一目标 |

### 6.2 引导采样 (radarGuide_calib)

**文件**：`ptz100_agx_fix/src/srv/algs/ai_alg/ai_main.cpp:837-917`

**当前逻辑**：
```
调用 radar_2ptz_calib 选取雷达目标
if 选取成功:
    记录雷达目标角度到全局变量 (target_horizatal_angle, target_pitching_angle, target_range)
    记录速度 (target_vx, target_vy, target_vz)
    （注意：setPTZ0 引导PTZ转向的代码被注释掉了）
```

数据采样在 `ai_alg_calib()` 的后半部分完成：
```
if mark_status == 1 且有雷达数据:
    写入 radar_ptz_status.txt:
        radar_azimuth, radar_elevation, range, vx, vy, vz,
        ptz_azimuth, ptz_elevation, timestamp, id, track_azimuth, track_elevation
```

#### 🔴 问题

| 问题 | 影响 | 建议 |
|------|------|------|
| setPTZ0 被注释掉 | 标定时不引导PTZ跟踪雷达目标，依赖PTZ自行AI检测锁定 | 标定时应该由雷达引导PTZ对准目标，或至少辅助引导 |
| 使用文件IO采样 | 高频写文件影响实时性；文件损坏则丢失所有数据 | 改为内存buffer采样，完成后一次性写入或直接在内存中计算 |
| 缺少采样质量评估 | 每个采样点都无条件记录 | 增加实时一致性检查：连续采样角度跳变>阈值时丢弃 |
| record_time 超时机制不健壮 | `AI_MARK_COLLECT_DATA_TIMEOUT` 从record_time开始计算，包含等待目标出现的时间 | 应从实际开始有效采样时计时 |

### 6.3 外参求解 (mainAPI / calib.cpp)

**文件**：`ptz100_agx_fix/src/srv/algs/ai_alg/calib.cpp:59-521`

**当前算法流程**：

```
1. 从文件读取 CSV 数据 (radar_azimuth, radar_elevation, range, ..., ptz_azimuth, ptz_elevation, ...)
2. 远离过程分段:
   - 按距离递增趋势分段
   - 相邻段角度跳变>阈值时切分
   - 计算各段方位/俯仰 vs 距离的斜率和截距
   - 合并相似段
   - 选择样本最多的段
   - 去除相邻帧角度差>1°的异常点
3. 靠近过程分段: (反向遍历，同样逻辑)
4. 合并远离/靠近两段数据
5. 计算方位/俯仰偏差:
   H_off = radar_azimuth - ptz_azimuth
   V_off = radar_elevation - ptz_elevation
6. 中值滤波: 排序后取中值
7. 阈值去异常: abs(偏差 - 中值) < f_TH(=10°)
8. 截尾均值: 取中间60%数据(20%-80%)求均值
9. 取反作为标定值: yaw_offset = -mean(H_off), pitch_offset = -mean(V_off)
10. 写入 calib.conf 文件
```

#### 🔴 问题

| 问题 | 严重度 | 详细说明 |
|------|--------|---------|
| **仅求解yaw/pitch，无roll/height** | 高 | 需求要求4个外参(yaw, pitch, roll, height_offset)，但算法只计算2个 |
| **简单差值法，非旋转矩阵拟合** | 高 | 直接用角度差均值作为外参，忽略了roll引起的交叉耦合（方位偏差随俯仰角变化） |
| **f_TH=10° 硬编码阈值** | 中 | 对于精标定场景，10°的异常阈值过大；应根据数据分布自适应 |
| **最少50个有效样本** | 中 | 硬编码 `v_azimuth_off_valid_size < 50` 返回失败，不可配置 |
| **文件读写作为数据接口** | 中 | 从文件读取CSV数据容易因格式错误崩溃（如 `std::stof` 抛异常） |
| **分段合并逻辑复杂且无文档** | 中 | 分段+合并+筛选逻辑约200行代码，完全没有注释说明目的 |
| **无距离加权** | 中 | 远距离采样点角度误差较大，应给予较低权重 |
| **截尾均值比例固定** | 低 | 使用20%-80%截尾均值，比例不可调整 |
| **角度wraparound处理不完整** | 低 | 仅在偏差>180°时+-360°，但分段逻辑中可能遗漏 |

#### 改进建议：外参求解算法

推荐的数学模型：

给定雷达目标在雷达坐标系下的方位角 \(a_r\)、俯仰角 \(e_r\)，以及 PTZ 实际锁定的方位角 \(a_p\)、俯仰角 \(e_p\)，建立模型：

$$
\begin{bmatrix} a_p \\ e_p \end{bmatrix} = R(\Delta\text{yaw}, \Delta\text{pitch}, \Delta\text{roll}) \cdot \begin{bmatrix} a_r \\ e_r \end{bmatrix} + \text{bias}
$$

对于小角度偏差，可简化为线性模型：

$$
a_p \approx a_r + \Delta\text{yaw} + \Delta\text{roll} \cdot \tan(e_r)
$$
$$
e_p \approx e_r + \Delta\text{pitch} + \Delta\text{height\_offset} / d
$$

其中 \(d\) 是目标距离。使用加权最小二乘法求解：

$$
\min \sum_{i} w_i \left[ (a_{p,i} - a_{r,i} - \Delta\text{yaw} - \Delta\text{roll} \cdot \tan(e_{r,i}))^2 + (e_{p,i} - e_{r,i} - \Delta\text{pitch} - \Delta h / d_i)^2 \right]
$$

权重 \(w_i\) 可基于距离和SNR设置。

---

## 7. 数据持久化与运行期应用

### 7.1 持久化现状

| 存储方式 | 内容 | 读写位置 | 问题 |
|---------|------|---------|------|
| SQLite `global_calibration` | 全局初值(经纬度/朝向/高度) | `alink_set_mark_param_new()` 写入 | 与YAML双写，冗余 |
| YAML配置文件 | 同上 + 设备属性 | `g_hostDevCfgApi.updateBySn()` 写入 | 已成为主要存储 |
| SQLite `radar_calibration` | 单雷达标定结果(local_delta_*) | `user_start_radar_calibration_result_handler()` 写入 | 但AI算法模块不写入此表 |
| 文件 `radar_ptz_status.txt` | 标定采样原始数据 | `ai_alg_calib()` 追加写入 | 临时文件，不可靠 |
| 文件 `calib.conf` | 标定结果(yaw_off, pitch_off) | `mainAPI()` → `write_calbvalue()` | 平面文件，无校验 |

### 7.2 运行期应用现状

**关键问题：标定结果在运行期引导中未被使用。**

`setPTZ0()` 函数（`ai_main.cpp:368-395`）直接将雷达角度转发给PTZ，**没有**加载或应用任何标定偏差补偿：

```c
void setPTZ0(float ablHor, float ablVer, float verticalHeight, 
             float horizontalDistance, ...)
{
    addr_t.guidance_mode = PTZ_ADDR_GUIDANCE_MODE_ANGLE;
    addr_t.horizatal_angle = ablHor;        // 未修正！
    addr_t.pitch_angle = ablVer;            // 未修正！
    addr_t.target_altitude = verticalHeight;
    addr_t.distance = horizontalDistance;
    send_ptz_control_addr(&addr_t);
}
```

`radarGuide()`（正常引导函数）调用 `setPTZ0(horAngle, obj_to_ptz.elevation, ...)` 时同样没有加入标定补偿。

#### 🔴 这意味着：即使完成了标定流程，标定结果也不会实际改善引导精度！

---

## 8. 需求与代码差距分析（GAP Analysis）

| # | 需求 | 当前代码状态 | 差距等级 |
|---|------|-------------|---------|
| 1 | 0x9A 消息（启停/管理方位角/查询历史） | ❌ 完全未实现 | **P0-阻塞** |
| 2 | 0x91 支持8种Request | ⚠️ 仅实现1-4，且映射关系与需求不一致 | **P0-阻塞** |
| 3 | 标定状态机含RADAR_MANUAL_SELECT | ❌ 状态未定义 | **P1-重要** |
| 4 | 0xE1 雷达原始目标类型 | ❌ ID被其他功能占用 | **P1-重要** |
| 5 | 标定结果含 roll_offset, height_offset | ❌ 算法仅计算yaw/pitch | **P1-重要** |
| 6 | 标定结果持久化到SQLite | ❌ 仅写入平面文件 | **P1-重要** |
| 7 | 标定结果运行期应用于引导 | ❌ setPTZ0 无偏差修正 | **P0-阻塞** |
| 8 | 中程雷达特化目标筛选 | ❌ 仅 alive>=0 | **P1-重要** |
| 9 | 自标定模块(calibr_alg)流程 | ⚠️ 代码用AI模块替代 | **P2-优化** |
| 10 | 标定数据实时上报C2画曲线(0x93) | ⚠️ 框架在但可能与AI模块不对接 | **P1-重要** |
| 11 | 近程/中程标定流程统型 | ⚠️ 控制流程基本一致，但消息定义不统一 | **P2-优化** |
| 12 | 双写(SQLite+YAML)清理 | ⚠️ 冗余存在 | **P3-技术债** |

---

## 9. 改进方案

### 9.1 P0 级改进（阻塞性问题）

#### 9.1.1 实现 0x9A 消息处理

在 `alink_track.cpp` 中新增注册和处理函数：

```c
// 新增 0x9A 消息结构
#pragma pack(1)
typedef struct alink_calibration_start_stop_req {
    char sn[25];
    uint8_t cmd;        // 1:启动 2:停止 3:添加方位 4:删除方位 5:查询历史
    float angle;        // 标定方位角度
    uint8_t reserve[128];
} alink_calibration_start_stop_req_t;
#pragma pack()

// 注册
alink_register_cmd(&pkg_0x9A, 0x9A, sizeof(alink_calibration_start_stop_req_t), 
                   handle_calibration_start_stop);

// 处理函数
static void handle_calibration_start_stop(alink_payload_t *psPayload, 
                                          alink_resp_t *psResp)
{
    auto* req = (alink_calibration_start_stop_req_t*)(psPayload->pbyData);
    switch (req->cmd) {
        case 1: // 启动标定
            // 复用原 user_start_radar_calibration 逻辑
            break;
        case 2: // 停止标定
            // 发送 RESULT_CANCELED 状态
            break;
        case 3: // 添加标定方位角
            // 存入 YAML/SQLite，关联 sn + angle
            break;
        case 4: // 删除标定方位角
            // 从存储中删除对应记录
            break;
        case 5: // 查询历史标定结果
            // 复用/增强原 get_radar_calibration_sql_data 逻辑
            break;
    }
}
```

#### 9.1.2 补全 0x91 处理

扩展 `radar_calibration_req()` 支持需求定义的所有Request值：

```c
static void radar_calibration_req(alink_payload_t *psPayload, alink_resp_t *psResp)
{
    struct alink_calibration_req* payload = ...;
    struct alink_calibration_rsp* resp = ...;
    
    switch (payload->Request) {
        case 1: // PTZ锁定确认
            ros_app_cmd_set(..., STATE_CODE_..._PTZ_TRACKING_CONFIRMED);
            break;
        case 2: // PTZ锁定取消
            ros_app_cmd_set(..., STATE_CODE_..._PTZ_TRACKING_CANCEL);
            break;
        case 3: // 视觉锁定目标确认 (新)
            ros_app_cmd_set(..., STATE_CODE_..._VISION_CONFIRMED);
            break;
        case 4: // 视觉锁定目标取消 (新) 
            ros_app_cmd_set(..., STATE_CODE_..._VISION_CANCEL);
            break;
        case 5: // 手动选择雷达目标模式 (新)
            ros_app_cmd_set(..., STATE_CODE_..._RADAR_MANUAL_SELECT);
            break;
        case 6: // 自动选择雷达目标模式 (新)
            ros_app_cmd_set(..., STATE_CODE_..._RADAR_AUTO_SELECT);
            break;
        case 7: // 标定结果确认
            ros_app_cmd_set(..., STATE_CODE_..._RESULT_CONFIRMED);
            break;
        case 8: // 标定结果取消
            ros_app_cmd_set(..., STATE_CODE_..._RESULT_CANCELED);
            break;
    }
}
```

#### 9.1.3 标定结果运行期应用

在 `setPTZ0()` 中加载并应用标定偏差：

```c
// 全局或模块级变量，启动时从 calib.conf/YAML/SQLite 加载
static float g_calib_yaw_offset = 0.0f;
static float g_calib_pitch_offset = 0.0f;
static float g_calib_roll_offset = 0.0f;
static float g_calib_height_offset = 0.0f;
static bool  g_calib_loaded = false;

void load_calibration_offsets(const char* sn)
{
    // 从 YAML 或 SQLite 加载指定雷达SN的标定结果
    // g_calib_yaw_offset = ...
    // g_calib_pitch_offset = ...
    g_calib_loaded = true;
}

void setPTZ0(float ablHor, float ablVer, float verticalHeight, 
             float horizontalDistance, ...)
{
    addr_t.guidance_mode = PTZ_ADDR_GUIDANCE_MODE_ANGLE;
    
    // ★ 应用标定偏差修正
    if (g_calib_loaded) {
        ablHor += g_calib_yaw_offset;
        ablVer += g_calib_pitch_offset;
        // roll 修正: ablHor += g_calib_roll_offset * tan(ablVer * DEG2RAD);
        // height 修正: ablVer += atan(g_calib_height_offset / horizontalDistance) * RAD2DEG;
    }
    
    addr_t.horizatal_angle = ablHor;
    addr_t.pitch_angle = ablVer;
    addr_t.target_altitude = verticalHeight;
    addr_t.distance = horizontalDistance;
    send_ptz_control_addr(&addr_t);
}
```

### 9.2 P1 级改进（重要功能）

#### 9.2.1 中程雷达特化目标筛选

增强 `radar_2ptz_calib()` 的筛选条件：

```c
int radar_2ptz_calib(std::vector<RadarObj> *obj_ptr, RadarObj &obj_to_ptz, int ptz_id)
{
    std::vector<RadarObj> useful_obj;
    
    for (auto it = obj_ptr->begin(); it != obj_ptr->end(); ++it) {
        // 基础过滤
        if (it->alive < 3) continue;           // 需要至少3帧存活
        
        // 中程特化过滤
        if (it->range < 200 || it->range > 5000) continue;   // 200m-5km
        if (abs(it->velocity) < 2.0f) continue;              // 速度>2m/s（排除静止物）
        if (it->elevation < -5.0f) continue;                  // 排除地面目标
        
        // 分类过滤（如果可用）
        // if (it->classification != CLASS_UAV) continue;
        
        useful_obj.push_back(*it);
    }
    
    // ... 后续选择逻辑
}
```

#### 9.2.2 改进外参求解算法

将 `mainAPI()` 重构为结构化的内存计算：

```c
struct CalibSample {
    float radar_az, radar_el, range;
    float ptz_az, ptz_el;
    uint64_t timestamp;
    int id;
    float weight;  // 基于距离/质量的权重
};

struct CalibResult {
    float yaw_offset;
    float pitch_offset;
    float roll_offset;
    float height_offset;
    float yaw_mean, yaw_var, yaw_max_diff;
    float pitch_mean, pitch_var, pitch_max_diff;
    int valid_sample_count;
    int valid_flag;  // CALIB_VALID_FLAG_YAW | CALIB_VALID_FLAG_PITCH
};

int compute_calibration(const std::vector<CalibSample>& samples, CalibResult& result)
{
    // 1. RANSAC/IRLS 去异常值
    // 2. 加权最小二乘拟合 yaw, pitch, roll, height_offset
    // 3. 计算残差统计
    // 4. 质量评估
}
```

#### 9.2.3 标定结果持久化

标定完成后，将结果同时写入 SQLite 和 YAML：

```c
// mark_task 中，mainAPI 成功后
radar_calibration_list_t cali_list = {0};
strncpy(cali_list.sn, calibration_sn, sizeof(cali_list.sn));
cali_list.timestamp = skyfend_get_systimemap();
cali_list.local_delta_yaw = f_H_off;
cali_list.local_delta_pitch = f_V_off;
cali_list.state_flag = 2; // 标定完成
write_radar_calibration(&cali_list);
```

### 9.3 P2 级改进（优化项）

1. **统一自标定模块与AI算法模块的标定职责**：明确由自标定模块管理状态机，AI算法模块只负责目标筛选和角度计算
2. **消除双写**：统一使用 YAML 存储全局标定参数，废弃 SQLite `global_calibration` 表
3. **增加标定日志**：在标定过程中输出结构化日志，便于问题诊断
4. **内存采样替代文件采样**：使用环形buffer替代每帧写文件
5. **自动化测试**：设计可复现的仿真测试用例

---

## 10. 实施路线图

### Phase 1：协议与框架对齐（预计2-3天）

| 任务 | 改动文件 | 说明 |
|------|---------|------|
| 实现 0x9A 消息处理 | `alink_track.cpp/.h` | 新增结构体、注册、处理函数 |
| 扩展 0x91 到8种Request | `alink_track.cpp` | 扩展 switch-case，新增 CmdEnum 定义 |
| 新增 CmdEnum 状态码 | `skyfend_interfaces` ROS包 | 添加 RADAR_MANUAL_SELECT 等 |
| 解决 0xE1 ID冲突 | 协调决定 | 可能需要分配新ID给雷达原始目标 |

### Phase 2：算法优化与补全（预计3-5天）

| 任务 | 改动文件 | 说明 |
|------|---------|------|
| 增强 radar_2ptz_calib 筛选 | `guide.cpp` | 添加距离/速度/分类过滤 |
| 重构 mainAPI 为内存计算 | `calib.cpp` | 去除文件IO，改为传参 |
| 实现 roll/height 求解 | `calib.cpp` | 加权最小二乘 |
| 采样数据改为内存buffer | `ai_main.cpp` | 替换 fopen/fprintf |
| 标定质量评估增强 | `calib.cpp` | 残差统计、自适应阈值 |

### Phase 3：持久化与运行期应用（预计2-3天）

| 任务 | 改动文件 | 说明 |
|------|---------|------|
| mark_task 结果写入 SQLite | `ai_main.cpp` | 调用 write_radar_calibration |
| setPTZ0 加载标定偏差 | `ai_main.cpp` | 系统启动时读取 calib.conf/SQLite |
| setPTZ0 应用偏差修正 | `ai_main.cpp` | 引导角度 += offset |
| 标定完成后自动重载 | `ai_main.cpp` | mark_task 结束时更新全局偏差 |

### Phase 4：联调与验证（预计3-5天）

| 任务 | 说明 |
|------|------|
| C2 0x9A 消息联调 | 与C2团队对接新消息 |
| 标定流程走通 | 单雷达+PTZ，完整标定流程 |
| 引导精度验证 | 标定前后对比引导到达角度误差 |
| 多方位标定测试 | 测试不同方位段的标定独立性 |
| 异常场景测试 | 超时、中途退出、多目标干扰等 |

---

## 11. 附录

### 附录A：关键数据结构

#### global_calibration_list_t (全局标定参数)
```c
typedef struct global_calibration_list {
    char sn[128];
    uint64_t timestamp;
    double longitude, latitude, altitude;
    float roll, pitch, yaw;
    int state_flag;        // 0-未初始化, 1-初始化, 2-标定完成
    float relative_height;
    struct global_calibration_list* next;
} global_calibration_list_t;
```

#### radar_calibration_list_t (单雷达标定结果)
```c
typedef struct radar_calibration_list {
    char sn[128];
    uint64_t timestamp;
    double longitude, latitude, altitude;
    float local_t_x, local_t_y, local_t_z;             // 平移
    float local_roll, local_pitch, local_yaw;           // 姿态
    float local_delta_t_x, local_delta_t_y, local_delta_t_z;     // 微调平移
    float local_delta_roll, local_delta_pitch, local_delta_yaw;   // 微调姿态（标定结果）
    int state_flag;
    struct radar_calibration_list* next;
} radar_calibration_list_t;
```

#### alink_calibration_status_t (0x92 标定状态上报)
```c
typedef struct alink_calibration_status {
    uint64_t timestamp;
    uint8_t Sn[25];
    uint8_t calib_ongoing_state;     // 见状态机章节
    uint8_t calib_state;
    float recommend_uav_height;
    float recommend_uav_fly_direction;
    uint8_t calib_progress_bar;      // 0-100
    uint8_t yaw_valid_flag;
    float yaw_mean, yaw_variance, yaw_max_diff;
    uint8_t pitch_valid_flag;
    float pitch_mean, pitch_variance, pitch_max_diff;
    float yaw_offset, pitch_offset, roll_offset, height_offset;
    uint8_t valid_flag;              // CALIB_VALID_FLAG_YAW=1, CALIB_VALID_FLAG_PITCH=2
    uint32_t Reserver;
} alink_calibration_status_t;
```

### 附录B：关键文件索引

| 文件路径 | 职责 |
|---------|------|
| `src/srv/alink/command/track/alink_track.cpp` | C2标定命令入口：0x71/0x91/0x92/0x93/0x94/0x95/0x96 |
| `src/srv/alink/command/track/alink_track.h` | 标定Alink消息结构体定义 |
| `src/app/ros_app/ros_app.cpp` | ROS桥接：UserCalibState订阅/CalibRadarPtzObjList回调 |
| `src/srv/algs/ai_alg/ai_main.cpp` | 标定主函数 ai_alg_calib()、数据采样、mark_task线程 |
| `src/srv/algs/ai_alg/guide.cpp` | radarGuide_calib() 目标选择、radar_2ptz_calib() 筛选 |
| `src/srv/algs/ai_alg/calib.cpp` | mainAPI() 外参求解算法 |
| `src/srv/algs/ai_alg/tf.cpp` | 坐标变换工具 |
| `src/common/sqlite/global_calibration.h/.c` | SQLite全局标定参数读写 |
| `src/common/sqlite/radar_calibration.h/.c` | SQLite单雷达标定结果读写 |

### 附录C：中程雷达 vs 近程雷达标定差异

| 特性 | 近程雷达（四面阵） | 中程雷达（机扫/相控阵） |
|------|-------------------|----------------------|
| 探测距离 | 50m - 2km | 200m - 8km |
| 角度精度 | ±0.5° | ±1° ~ ±2° |
| 波束宽度 | 窄 | 较宽 |
| 更新率 | 高（4面同时） | 较低（扫描周期） |
| 标定方位段 | 4面各自标定 | 全方位或分段标定 |
| 对标定算法影响 | 角度差直接可用 | 需要更多样本、更强去噪、距离加权 |

---

> **总结**：当前代码已具备基本的标定框架（Alink消息传递、ROS桥接、简单的外参计算），但存在多个阻塞性问题：0x9A未实现、0x91不完整、标定结果未在运行期应用、仅计算yaw/pitch两个外参。建议按照 Phase 1→4 的路线图依次推进，优先解决 P0 级问题以打通端到端流程，再逐步优化算法精度和健壮性。

