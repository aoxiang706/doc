---
description: 代码审查检查清单和反馈格式
alwaysApply: false
---

# 代码审查（中文版）

## 审查检查清单

### 功能正确性
- [ ] 逻辑是否正确
- [ ] 边界条件是否处理
- [ ] 错误情况是否处理
- [ ] 是否满足需求

### 代码质量
- [ ] 代码是否清晰易读
- [ ] 命名是否有意义
- [ ] 函数是否过长（建议 < 50 行）
- [ ] 是否有重复代码
- [ ] 注释是否必要且准确

### 安全性
- [ ] 输入是否验证
- [ ] 是否有缓冲区溢出风险
- [ ] 是否有内存泄漏
- [ ] 敏感信息是否保护

### 性能
- [ ] 是否有不必要的循环/拷贝
- [ ] 算法复杂度是否合理
- [ ] 是否有资源泄漏

### 线程安全
- [ ] 共享资源是否保护
- [ ] 是否有死锁风险
- [ ] 回调是否线程安全

---

## 反馈格式

### 严重程度

| 标记 | 含义 | 说明 |
|------|------|------|
| 🔴 **必须修复** | Blocker | 不修复不能合并 |
| 🟡 **建议修改** | Suggestion | 强烈建议但不阻塞 |
| 🟢 **可选改进** | Nice to have | 可以后续优化 |
| 💬 **讨论** | Question | 需要讨论的点 |
| 👍 **点赞** | Praise | 好的实现 |

### 反馈示例

```markdown
🔴 **必须修复**: 这里有空指针解引用风险

`ptr` 可能为 null，需要添加检查：

```cpp
if (ptr != nullptr) {
    ptr->doSomething();
}
```

---

🟡 **建议修改**: 这个函数太长了

建议拆分为：
1. `parseHeader()` - 解析头部
2. `parseBody()` - 解析内容
3. `validateResult()` - 验证结果

---

🟢 **可选改进**: 可以使用 `std::optional` 替代返回指针

---

👍 很好的错误处理，覆盖了所有边界情况！
```

---

## 审查流程

1. **理解背景**：先理解这个 MR/PR 要解决什么问题
2. **整体浏览**：快速看一遍所有改动
3. **逐文件审查**：按文件详细审查
4. **运行测试**：确保测试通过
5. **给出反馈**：按格式给出具体反馈
6. **跟进修改**：确认修改是否满足要求
