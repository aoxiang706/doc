---
description: Bug 修复标准流程，确保问题彻底解决
alwaysApply: false
---

# Bug 修复流程（中文版）

## 修复流程

### 第一步：复现问题

```bash
# 1. 记录复现步骤
# 2. 确认复现条件（环境、输入、时序）
# 3. 记录错误日志/堆栈
```

### 第二步：定位问题

```bash
# GDB 调试
ros2 run --prefix 'gdb -ex run --args' <package> <node>

# 段错误时获取堆栈
(gdb) bt

# 添加日志
RCLCPP_ERROR(this->get_logger(), "xxx: %d", value);
```

### 第三步：分析根因

使用「五问法」深入分析：
- 不止修复症状，找到根本原因
- 考虑是否有类似问题

### 第四步：编写修复

```cpp
// ❌ 错误：只修复症状
if (ptr != nullptr) {  // 加个空指针检查
    ptr->doSomething();
}

// ✅ 正确：修复根因
// 确保 ptr 在使用前已正确初始化
ptr = createValidPointer();
ptr->doSomething();
```

### 第五步：验证修复

- [ ] 原问题不再复现
- [ ] 没有引入新问题
- [ ] 相关功能正常
- [ ] 编译无警告

### 第六步：提交修复

```bash
gadd <修改的文件>
gcommit -m "fix(scope): 简短描述问题和修复"
```

---

## 常见 Bug 类型

### 1. 段错误（SIGSEGV）

**常见原因**：
- 空指针解引用
- 数组越界
- 野指针（悬垂指针）
- 多线程竞争

**调试方法**：
```bash
# GDB 获取堆栈
(gdb) bt

# 检查指针值
(gdb) print ptr
(gdb) info locals
```

### 2. 内存泄漏

**调试方法**：
```bash
valgrind --leak-check=full ./program
```

### 3. 死锁

**常见原因**：
- 锁的获取顺序不一致
- 忘记释放锁
- 递归锁使用错误

**调试方法**：
```bash
# 查看线程状态
(gdb) info threads
(gdb) thread apply all bt
```

### 4. 通信超时

**排查方向**：
- 网络连通性
- 端口占用
- 缓冲区满
- 协议不匹配

---

## 修复检查清单

- [ ] 问题根因已明确
- [ ] 修复代码简洁有效
- [ ] 添加了必要的注释说明
- [ ] 没有引入新的警告
- [ ] 测试通过
- [ ] 提交消息清晰描述了修复内容
